const config = {
    // 根路由ypd(优排队)
    rootpath: '/ypd',
    // 启动端口
    port: 3001,
    // 数据库配置
    database:{
        DATABASE: 'youpaidui',
        USERNAMW: 'root',
        PASSWORD: '123456',
        PORT: '3306',
        HOST: 'localhost'
    }

}

module.exports = config;